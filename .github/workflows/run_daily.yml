name: Run daily script and update Dashboard

on:
  schedule:
    - cron: "30 12 * * *"   # 09:30 AM Argentina
  workflow_dispatch:

permissions:
  contents: write

jobs:
  run:
    runs-on: ubuntu-latest
    steps:
      - name: Checkout repo
        uses: actions/checkout@v4
        with:
          fetch-depth: 0

      - name: Set up Python
        uses: actions/setup-python@v5
        with:
          python-version: "3.11"

      - name: Install dependencies
        run: |
          pip install requests pandas matplotlib seaborn xlsxwriter

      - name: Run script
        env:
          EMAIL_USER: ${{ secrets.EMAIL_USER }}
          EMAIL_PASSWORD: ${{ secrets.EMAIL_PASSWORD }}
          EMAIL_RECIPIENT: ${{ secrets.EMAIL_RECIPIENT }}
        run: python bigmac.py

      - name: Commit and Push
        run: |
          git config user.name "github-actions[bot]"
          git config user.email "github-actions[bot]@users.noreply.github.com"
          
          # Añadir el CSV y la carpeta docs (web e imágenes)
          git add mcdonalds_precios.csv docs/
          
          if git diff --staged --quiet; then
            echo "No hay cambios para commitear"
          else
            git commit -m "Update prices and Dashboard (daily)"
            git push
          fi
          
